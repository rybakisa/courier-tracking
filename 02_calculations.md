# Система трекинга курьеров — Артефакт 2. Расчёты

Учебный проект по системному дизайну. Документ оценивает нагрузку порядками величин:
RPS, объём хранилища, пропускную способность, fan-out и число серверов.

---

## 1. Исходные данные и допущения

| Параметр | Значение | Источник |
|---|---|---|
| Зарегистрировано курьеров | 500 000 | Требования |
| Пик одновременно онлайн | 300 000 | Требования |
| Среднее одновременно онлайн | ~200 000 | Смена ~10 ч из 24 ч |
| Интервал «живого» пинга | 4 с | Допущение (3–5 с, как в индустрии) |
| Пик клиентских сессий слежения | 200 000 | Требования |
| Пик диспетчерских сессий | 5 000 | Требования |
| Размер сообщения на проводе | ~50 Б | Бинарный формат + фрейминг |
| Размер точки в хранилище | ~20 Б | Упакованная запись до компрессии |
| Курьеров в диспетчерском вьюпорте | ~1 000 средн. / 10 000 макс. | Требования |
| Интервал обновления карты диспетчера | 5 с | Допущение |

### Уточнение модели относительно черновика расчёта

В первом варианте батчинг делало **устройство** — копило 60 точек и слало раз в минуту.
Это срезает RPS, но раз-в-минуту несовместимо с суб-секундным NFR-1: сервер узнавал бы
позицию раз в минуту. В финальной модели:

- **устройство** держит постоянное соединение и шлёт частый поток (раз в 4 с) — это
  удовлетворяет NFR-1, плавность движения точки добирается клиентской интерполяцией;
- **батчинг переезжает на сервер** — консьюмер копит точки и пишет в холодное хранилище
  большими пачками. Идея батча сохранена, но на правильном слое.

---

## 2. Модель потоков данных

Один поток на запись разветвляется на сервере на «живой» и «холодный»:

```
Курьер ──(пинг раз в 4 с)──▶ Лог-брокер ──┬──▶ Живой путь   ──▶ fan-out клиентам/диспетчерам
                                          └──▶ Холодный путь ──▶ батч-запись в историю
```

- **Живой путь** несёт NFR-1 (свежесть). Считаем message rate и fan-out.
- **Холодный путь** несёт NFR-2 (хранение 1 год). Считаем объём.

---

## 3. RPS / message rate

```
Пиковый ingest (живой):  300 000 курьеров ÷ 4 с      = 75 000 сообщений/с
Средний ingest:          200 000 курьеров ÷ 4 с      = 50 000 сообщений/с
Проектная мощность ×2:                               = 150 000 сообщений/с

Запись в историю:        батч по 5 000 точек         → 75 000 ÷ 5 000 ≈ 15 батч-операций/с
Чтение истории (FR-4/5): редкие запросы              ≈ 3 RPS (пренебрежимо по нагрузке)
```

Главное число — **75 000 сообщений/с на пике**, проектируем на **150 000/с**.
RPS HTTP-запросов как метрика здесь не применяется: и курьеры, и клиенты сидят на
**постоянных соединениях**, поэтому считаем сообщения/с, а не запросы/с.

---

## 4. Объём хранилища (Volume)

```
Точек на курьера за смену:   36 000 с (10 ч) ÷ 4 с        = 9 000 точек
На курьера в день:           9 000 × 20 Б                 = 180 КБ
Все курьеры в день:          500 000 × 180 КБ             = 90 ГБ/день (сырьё)
За год (NFR — 1 год):        90 ГБ × 365                  ≈ 33 ТБ/год (сырьё)
После компрессии (~6×):      delta-of-delta по коорд./вр. ≈ 5.5 ТБ/год
С репликацией ×3:                                         ≈ 16–17 ТБ/год на дисках
```

**Чувствительность к гранулярности** (открытый вопрос из требований). Принято: хранить
с частотой живого пинга — раз в 4 с. Если для точности маршрута и выплат нужна
**секундная** гранулярность, объём растёт ×4:

| Гранулярность | Сырьё/год | Сжато/год | С репликацией ×3 |
|---|---|---|---|
| 1 точка / 4 с (принято) | ~33 ТБ | ~5.5 ТБ | ~16 ТБ |
| 1 точка / 1 с | ~130 ТБ | ~22 ТБ | ~66 ТБ |

---

## 5. Пропускная способность (Throughput / сеть)

```
Живой ingest:            75 000 сообщ./с × 50 Б    = 3.75 МБ/с  ≈ 30 Мбит/с
Fan-out клиентам:        50 000 сообщ./с × 50 Б    = 2.5 МБ/с   ≈ 20 Мбит/с
Fan-out диспетчерам:     см. раздел 6 (с кластер.) = 9 МБ/с     ≈ 72 Мбит/с
Запись в хранилище (I/O):75 000 точек/с × 20 Б     = 1.5 МБ/с
──────────────────────────────────────────────────────────────────────────
Итого по сети (устойчиво):                         ≈ 15 МБ/с   ≈ 120 Мбит/с
С учётом overhead TCP/WS (×1.5–2) и запаса ×2:     планировать ≈ 250–300 Мбит/с
```

Вывод: **сеть здесь не узкое место.** Узкие места — число соединений и CPU на fan-out.

---

## 6. Fan-out и нагрузка чтения

Fan-out factor — на скольких потребителей размножается один апдейт. Это, а не «read ≈ write»,
определяет нагрузку чтения.

**Клиентский флоу — fan-out ≈ 1.** Один курьер → один клиент. Поэтому чтение
сопоставимо с записью: 50 000 сообщ./с, ~20 Мбит/с.

**Диспетчерский флоу — fan-out большой и опасный.** Один курьер в плотном центре попадает
в десятки вьюпортов:

```
Без кластеризации (средний вьюпорт):
  5 000 дисп. × 1 000 курьеров ÷ 5 с × 50 Б   = 50 МБ/с  (400 Мбит/с)
Без кластеризации (худший вьюпорт 10 000):
                                              ≈ 500 МБ/с (4 Гбит/с)  ← неприемлемо

С серверной кластеризацией (~150 маркеров на вьюпорт независимо от плотности):
  5 000 дисп. × 150 маркеров ÷ 5 с × 60 Б     = 9 МБ/с   (72 Мбит/с)  ← приемлемо
```

**Вывод:** диспетчерским флоу можно пренебречь в расчётах **не потому что диспетчеров мало**,
а потому что серверная кластеризация плотных вьюпортов схлопывает fan-out в ~7–50 раз.
Это обязательное архитектурное допущение, а не упрощение.

---

## 7. Соединения и число серверов

```
Постоянные соединения курьеров:    300 000
WebSocket-соединения клиентов:     200 000
WebSocket-соединения диспетчеров:    5 000
─────────────────────────────────────────
Всего одновременных соединений:   ≈ 505 000

На один gateway-узел (консервативно): 20 000 соединений
Узлов:        505 000 ÷ 20 000 ≈ 26
С резервом и запасом на пик:        ≈ 35–40 узлов
```

Это и есть реальный сайзинг gateway-тира — вывод, которого без подсчёта соединений не получить.

---

## 8. Итоговые числа для проектирования

| Метрика | Значение | Замечание |
|---|---|---|
| Пиковый ingest | ~75 000 сообщ./с (проект — 150 000/с) | Главный драйвер ingestion-слоя |
| Одновременных соединений | ~505 000 | → 35–40 gateway-узлов |
| Хранилище | ~33 ТБ/год сырьё, ~16 ТБ с компрессией и репликацией | При гранулярности 4 с; ×4 при 1 с |
| Запись в историю | ~15 батч-операций/с | Батчинг на стороне сервера |
| Fan-out клиентам | ~50 000 сообщ./с, fan-out ≈ 1 | Дёшево |
| Fan-out диспетчерам | ~9 МБ/с **с кластеризацией** | Без неё — до 4 Гбит/с |
| Сеть | ~250–300 Мбит/с на пике | Не узкое место |
| Узкие места | число соединений, CPU на fan-out, кластеризация | — |
