# 1. Мониторинг

Принцип: **каждая метрика привязана к требованию**. Не меряем «вообще здоровье», а отвечаем на
вопрос «нарушаем ли мы NFR прямо сейчас и куда расходуется бюджет ошибок». Золотой сигнал системы —
сквозная свежесть (ping → экран): именно её обещают клиенту, и именно она первой деградирует под
нагрузкой из артефакта 02.

Разделение горячего и холодного пути (артефакт 05, §2) даёт и разделение наблюдаемости: у горячего
пути симптом — **задержка**, у холодного — **потеря/отставание**. Их меряем и алертим раздельно.

---

# 2. Ключевые метрики

| Метрика | Как меряем | Требование | Порог алерта |
|---|---|---|---|
| **E2E-свежесть** ping→экран | `ts` доставки наблюдателю − `ts` пинга; p50/p95/p99 по флоу | NFR1 | p99 клиента > 1 с; любой > 60 с — критично |
| **Лаг консьюмеров** | offset head − offset консьюмера (live и history раздельно) | NFR1 / NFR2 | live > 2 с; history растёт монотонно |
| **Соединения и gateway** | активные WS/узел, churn реконнектов, CPU/mem, push-сообщ/с | NFR5 (ёмкость) | > 18K conn/узел (потолок 20K) |
| **Потеря пингов** | разрывы в `seq` на курьера, доля unacked-переотправок | NFR2 | > 0 после durable-`ack` |
| **Out-of-order / дедуп** | доля пингов вне порядка, доля отсеянных дублей | NFR3 | аномальный рост доли |

## 2.1 Декомпозиция E2E-свежести

Одно число p99 не говорит, *где* затык. Поэтому E2E-задержку разбиваем по стадиям горячего пути и
ведём бюджет по каждой:

```
  пинг курьера
      │  ① сеть курьер→gateway
      ▼
  gateway ──② ingest+дедуп──▶ лог ──③ append/durable-ack──▶
      │
      ④ live-консьюмер забрал из лога
      ▼
  live-store upsert ──⑤ fan-out/кластеризация──▶
      │
      ⑥ сеть gateway→наблюдатель
      ▼
  кадр на экране
```

Суммарный бюджет клиента — суб-секунда; внутри неё ② ③ ④ ⑤ — единицы–десятки мс, основной вклад дают
сетевые ① и ⑥ (мобильный канал). Если p99 уехал — сразу видно, какая стадия: лаг лога (③④), насыщение
fan-out (⑤) или деградация сети. Реализуется трассировкой с семплированием (один из N пингов несёт
trace-id через все стадии).

## 2.2 Горячий vs холодный — разные симптомы

- **Горячий путь** деградирует *тихо* (растёт задержка, картинка «лагает»), поэтому главный сигнал —
  перцентили свежести и **live-лаг**, а не доступность.
- **Холодный путь** не имеет права терять (NFR2): здесь главный сигнал — **history-лаг** (растёт →
  риск durability) и нулевая потеря пингов после `ack`. Скорость холодного пути не важна, важна
  полнота.

---

# 3. SLO и бюджет ошибок

Три SLO, по одному на ключевой NFR. Бюджет ошибок — то, что переводит «99.99%» в число минут, которые
можно потратить, прежде чем замораживать релизы.

| SLO | Цель | Окно | Бюджет ошибок |
|---|---|---|---|
| **Доступность** клиентского read-пути | 99.99% (NFR5) | 30 дней | ≈4.3 мин/мес (≈52 мин/год) |
| **Свежесть** клиентского флоу | 99% кадров < 1 с | 30 дней | 1% кадров вне цели |
| **Durability** ingest | 0 потерянных пингов после `ack` | всегда | бюджета нет — жёсткий инвариант |

Диспетчерский флоу — отдельный, более мягкий SLO по свежести (секунды, не суб-секунда; NFR1). История
к задержке не относится — для неё SLO про полноту, не про latency.

**Что ест бюджет.** Деградация `503 DEGRADED` с откатом на чтение последней точки (артефакт 03 §5) —
это **частичный** расход бюджета доступности, не полный отказ: клиент видит последнюю позицию, теряет
лишь живость. Это сознательный размен из требований — durability важнее живости.

---

# 4. Алерты

Алертим на **симптомы у пользователя**, а не на каждую внутреннюю метрику, — иначе дежурный тонет в
шуме. Внутренние метрики (лаг, CPU) идут на дашборды и во вторичные алерты для диагностики.

| Алерт | Условие | Почему симптом |
|---|---|---|
| Свежесть деградирует | burn-rate бюджета свежести × фоновой (мульти-окно: 5 мин + 1 ч) | клиент видит лаги «здесь и сейчас» |
| Durability под угрозой | history-лаг растёт монотонно ИЛИ потеря пингов > 0 | риск необратимой потери истории |
| Gateway насыщается | conn/узел > 18K ИЛИ всплеск реконнектов | предвестник отказа в соединении |
| Live-лаг | live-консьюмер > 2 с за head | горячая картинка «стареет» |

**Burn-rate, а не порог.** Алертим на скорость сжигания бюджета (быстрый расход → страница дежурному,
медленный → тикет), а не на статичный порог «p99 > X»: так ловим и резкий инцидент, и медленную утечку
бюджета, не дёргая дежурного по разовым выбросам.

---

# 5. Что на дашбордах

Контекст, без которого метрики не читаются (не алертим, но смотрим при разборе):

- **Бизнес-пульс:** онлайн-курьеры, активные клиентские/диспетчерские сессии — знаменатель для всех
  нагрузочных метрик и проверка расчётов из артефакта 02 на реальных числах.
- **География:** свежесть и нагрузка в разрезе регионов (NFR4) — деградация часто локальна (один
  регион/зона), агрегат по миру её прячет.
- **Hot-доля:** сколько курьеров «горячие» (под наблюдением, частота ≈4 с) против фоновых — прямой
  драйвер fan-out и ingest-нагрузки (артефакт 03 §2).

**Вывод.** Наблюдаемость повторяет архитектуру: золотой сигнал — E2E-свежесть, разобранная по стадиям
горячего пути; durability холодного пути меряется отдельно и не прощает потерь; алерты — на burn-rate
пользовательских SLO, остальное — на дашборды для диагностики. Этим семь артефактов замыкаются: от
требований (01) через расчёты (02) и контракты (03) к архитектуре (04–06) и к доказательству, что она
держит обещания, в проде (07).
